#!/usr/bin/env python3
"""Simple CLI for the hello package."""
import sys
from hello import greet


def main():
    name = sys.argv[1] if len(sys.argv) > 1 else "World"
    print(greet(name))


if __name__ == "__main__":
    main()
